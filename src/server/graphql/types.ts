import { userTypes } from "../types/userTypes";
import { projectTypes } from "../types/projectTypes";
import { authTypes } from "../types/authTypes";
import { advanceProjectTypes } from "../types/advanceProjectTypes";

export const types = [
    userTypes, 
    projectTypes, 
    authTypes,
    advanceProjectTypes
];